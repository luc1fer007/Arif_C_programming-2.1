#include<stdio.h>
int main()
{
    int n;
    printf("Enter n: ");
    scanf("%i", &n);           // in int (%d) or %i can be used///

    printf("%x\n", n);         //%x is for hexa and  
    printf("%o", n);                         //%o is for octal value//
    return 007;
}